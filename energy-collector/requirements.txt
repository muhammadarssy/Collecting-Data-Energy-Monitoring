pymodbus==3.6.9
asyncpg>=0.29
pydantic-settings>=2.0
PyYAML>=6.0
structlog>=24.1
redis>=5.0
pyserial==3.5
# Hanya ARM (Raspberry Pi). Mini PC x86_64 jangan install paket ini.
RPi.GPIO==0.7.1; sys_platform == "linux" and (platform_machine == "armv7l" or platform_machine == "aarch64")

# GPIO — rpi-lgpio adalah drop-in replacement RPi.GPIO (import tetap sama).
# Wajib di Pi OS Bookworm / kernel 6.6+; RPi.GPIO klasik gagal add_event_detect.
#
# Cara A — DISARANKAN (via apt):
#   sudo apt remove python3-rpi.gpio
#   sudo apt install python3-rpi-lgpio
#
# Cara B — via pip:
#   pip install rpi-lgpio
# rpi-lgpio>=0.6; platform_machine == "armv7l" or platform_machine == "aarch64"
